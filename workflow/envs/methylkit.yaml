name: methylkit

channels:
  - conda-forge
  - bioconda

channel_priority: strict

dependencies:
  - ca-certificates
  - certifi
  - openssl
  - curl
  - htslib
  - tabix

  - r-base=4.3
  - bioconductor-methylkit

  - r-data.table
  - r-optparse
  - r-readr
  - r-stringr
